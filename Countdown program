# Countdown program

# Ask the user to enter a positive integer
n = int(input("Please enter a positive integer:"))

# Use a while loop to count backwards from n to 1
while n >= 1:
    print(n)
    n = n - 1
